# budwk-docs
BudWk 开发指南

# Install

`npm i`

# Dev

`export NODE_OPTIONS=--openssl-legacy-provider`

`npm run docs:dev`

# Build

`export NODE_OPTIONS=--openssl-legacy-provider`

`cd docs`

`npm run docs:build`
